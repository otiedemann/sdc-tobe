"""Modularized C2 web controller — layered into config / state / model /
controller / api / view subpackages. PR #4 will add a Flask app factory
here (`build_app`); for now the entrypoint script remains
`remote_web_controller.py` next to this file."""
