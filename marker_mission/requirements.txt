opencv-contrib-python>=4.8
numpy>=1.24
flask>=2.2
requests>=2.28
# Optional: only used by tools/replay_kalman.py for the A/B plot.
# The CSV output works without it; a missing matplotlib triggers a
# graceful warning instead of a crash.
matplotlib>=3.5
# Parrot Olympe SDK — needed because marker_mission.app's combined
# entry point imports controller_unified.unified_api_server in-process,
# which Olympe-connects to the drone. Without this the FC service
# crashes at startup with `ERROR: olympe not installed.` and falls
# into a systemd Restart=on-failure loop.
parrot-olympe>=7.7
